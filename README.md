# Student inormation Manager on python

This is a system in console that manage general student information. 

The fundamental concepts from python are applied (Dictionaries, lists, exceptions, data export, basic variables).

Let the user manage student information on memory and secure persistance data, exporting and importing CSV files.
